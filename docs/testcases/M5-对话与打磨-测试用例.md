# Recall - M5 对话与打磨 测试用例文档

| 项 | 内容 |
|---|---|
| 里程碑 | M5 对话与打磨（T-M5-01 ~ T-M5-08） |
| 对应验收 | PRD 7.8（24-26）、7.9（27-28）；UI/UX 3.3 AI 答疑双栏、3.7 帮助中心、第九章响应式 |
| 测试范围 | AI 答疑 SSE 流式与停止/继续、会话历史管理、加入错题本、帮助中心、响应式三档、万级压测、安全隐私、全流程 E2E |
| 测试层次 | 后端 Pytest、前端 Vitest、Playwright E2E、性能脚本、人工核查 |
| 日期 | 2026-08-15 |

## 1. 环境与前置条件

- 后端已启动（mock 模式）；数据含 10000+ 压测错题
- Playwright Chromium 已安装（`npx playwright install chromium`）

## 2. 测试用例

### 2.1 后端（Pytest）

| 用例编号 | 用例名称 | 操作步骤 | 预期结果 | PRD 验收 |
|---|---|---|---|---|
| M5-BE-001 | SSE 流式 token | POST /chat（stream） | content-type=text/event-stream；token 帧逐段 delta；done 帧含 conversation_id/message_id | 7.8-24 |
| M5-BE-002 | 空输入拦截 | content="" | 422 | 5.8 |
| M5-BE-003 | 会话自动创建 | 无 conversation_id 发问 | 新会话 conv_ 前缀；user+assistant 两条消息入库 | 7.8-24 |
| M5-BE-004 | 会话 CRUD | 新建/重命名/列表 | 标题更新生效；列表返回 message_count | 5.8 |
| M5-BE-005 | 会话搜索 | ?q=电磁 | 仅命中标题含关键词的会话 | 5.8 |
| M5-BE-006 | 清空会话 | POST /conversations/{id}/clear | 消息清空、会话保留 | 5.8 |
| M5-BE-007 | 删除会话 | DELETE | 会话与消息移除；404 后续访问 | 7.8-26 |
| M5-BE-008 | 题目提取草稿 | POST /chat/extract（mock） | 题干=用户提问、解析=AI 讲解摘要、答案为空（提示补充） | 7.2-7 |
| M5-BE-009 | 补答案归档 | 草稿+答案 → POST /mistakes | 201，source=chat | 7.2-7 |
| M5-BE-010 | 删会话不影响错题本 | 归档后删除会话 | 错题本数据完好（PRD 7.8-26） | 7.8-26 |

### 2.2 前端（Vitest）

| 用例编号 | 用例名称 | 预期结果 |
|---|---|---|
| M5-FE-001 | 发送时用户消息先行上屏 + token 流式累加 | 消息数组 2 条；assistant 内容逐段累加、streaming=true |
| M5-FE-002 | done 结束流式 | streaming=false；conversationId 记录；会话列表刷新 |
| M5-FE-003 | error 保留已渲染内容 | 中断后已输出内容不丢失、显示统一兜底文案 |
| M5-FE-004 | 停止生成 | abort 调用；streaming 复位；空内容显示（已停止） |
| M5-FE-005 | 提取草稿 | draft 内容与 mock 结果一致 |

### 2.3 性能压测（scripts/perf_test.py，10000 条实测）

| 指标 | 实测（均值/P95） | 目标 | 结论 |
|---|---|---|---|
| 列表分页 | 9.6ms / 25.3ms | < 500ms | ✅ |
| 关键词搜索（LIKE） | 21.1ms / 24.8ms | < 500ms | ✅ |
| 组合筛选 | 12.6ms / 14.9ms | < 500ms | ✅ |
| 标签筛选 | 10.0ms / 12.5ms | < 500ms | ✅ |
| 看板聚合 7d | 19.3ms / 87.7ms | < 2s | ✅ |
| 看板聚合 30d | 14.6ms / 63.9ms | < 2s | ✅ |
| 知识图谱 | 10.5ms / 40.6ms | < 2s | ✅ |
| 导出 Markdown（200 题） | 99.0ms | 可用 | ✅ |
| 今日计划 | 17.6ms / 21.3ms | < 500ms | ✅ |

> 压测中实测发现并修复：业务 ID 由 4 字节 hex（32 位）升为 8 字节（64 位），万级批量插入不再碰撞（gen_id）。

### 2.4 E2E（Playwright，e2e/core-flow.spec.ts）

| 用例编号 | 用例名称 | 覆盖环节 | PRD 验收 |
|---|---|---|---|
| M5-E2E-001 | 核心闭环 | 登录（首次揭示/已有令牌）→ 文本录入 → 错题本搜索与详情 → 一键复习（选学科/作答/交卷/EX-08 弹窗/批改结果）→ 完成更新计划 → 看板三卡渲染 | 7.1~7.9 主流程 |
| M5-E2E-002 | 空状态与清除筛选 | 搜索无结果 → 「没有找到相关错题」+ 清除筛选条件 | 7.3-9 / 7.1 |

### 2.5 UI/UX 合规核查（人工）

| 用例编号 | 检查项 | 验收条件 | UI/UX 依据 |
|---|---|---|---|
| M5-UI-001 | AI 答疑双栏 | 260px 会话历史 + 问答区；消息流居中 max 760px；平板 220px；移动单栏 | 3.3 / 9.3 |
| M5-UI-002 | 消息气泡 | 用户右侧品牌 8% 底；AI 左侧白卡；流式打字光标 blink 1s | 4.3 |
| M5-UI-003 | 停止/继续 | 输出中「停止」按钮；中断后保留内容 +「继续生成」 | 5.4 / 7.8-24 |
| M5-UI-004 | 加入错题本 | 回答完成后按钮 → 草稿弹窗（题干+解析预填、缺答案红字提示）→ 确认归档 | 5.1.3 |
| M5-UI-005 | 会话管理 | 悬停显示 重命名/清空/删除；操作均二次确认 | 4.3 / 5.2 |
| M5-UI-006 | 输入区 | textarea min 48px max 160px 自适应；空输入禁发；Ctrl+Enter 发送 | 4.3 / 5.4 |
| M5-UI-007 | 帮助中心双栏 | 220px 导航 + 内容 760px；平板顶部 Tab；移动手风琴 | 3.7 / 9.3 |
| M5-UI-008 | 新手引导 4 步 | 录入→归档→复习→看板，每步可点击直达对应页面 | 4.7 / 6.1 |
| M5-UI-009 | FAQ 手风琴 | 7 项 FAQ 覆盖文档要求主题；同时只展开一项 | 4.7 / 5.3 |
| M5-UI-010 | 快捷键表 | /、Ctrl+E、Ctrl+Enter、Alt+R、Esc | 4.7 / 5.4 |
| M5-UI-011 | 数据流向说明 | 帮助「版本信息」与设置页明示：本地存储、OCR 本地、仅题干发 AI、127.0.0.1 | 5.5 / 4.7 |
| M5-UI-012 | 响应式三档 | 1200/1024/375 无横向溢出；移动端底部导航 56px、触控目标 ≥44px | 第九章 |
| M5-UI-013 | 专注舱（答疑） | Moon 按钮切换深色舱位，滚动与输入内容保留 | 2.4 |
| M5-UI-014 | 首屏分块 | echarts/markdown/icons/vendor manualChunks 拆分（NFR-04 首屏 <2s 支撑） | 6.1 |

### 2.6 安全与隐私核查（T-M5-07）

| 检查项 | 实现 | 依据 |
|---|---|---|
| API Key 加密落盘 | Fernet 本机密钥（data/auth/secret.key），前端仅掩码 | 规划 5.5 |
| 默认监听 127.0.0.1 | config 默认 host；设置页 LAN 开关明示 | 规划 5.5 |
| 图片不外发 | OCR 本地（mock/Paddle 本机 HTTP）；对话 attachments 不发送 | PRD 6.3 |
| 仅题干发 AI | 拆题/归档/批改/答疑 prompt 仅含文本；设置页可关闭 | PRD 6.3 |
| 令牌恒定时间比较 | secrets.compare_digest；登录限流（10 次/分钟） | 规划 5.5 |
| 错误不暴露堆栈 | 统一异常处理器；结构化日志不含敏感信息 | 规划 5.3 |

## 3. 执行结果

| 层 | 工具 | 用例数 | 通过 | 失败 |
|---|---|---|---|---|
| 后端 | Pytest | 97 | 97 | 0 |
| 前端 | Vitest | 37 | 37 | 0 |
| E2E | Playwright | 2 场景 | 通过 | 0 |
| 性能 | perf_test.py（10000 条） | 9 项指标 | 全部达标 | 0 |
| 构建 | vue-tsc + vite build | — | 通过 | 0 |

## 4. 里程碑收尾

1. 全部里程碑 M0~M5 交付：主干可运行（start 脚本 / Docker）、mock 模式全流程可演示；
2. OCR 20 并发压测（PRD 7.9-27）：单机串行队列 + 幂等已实现，接入真实 PaddleOCR 后按 perf_test 扩展并发脚本复测；
3. 真实 DeepSeek 接入：设置页配置 Key 后关闭 mock（RECALL_AI_MOCK=false 重启），拆题/归档/变体/批改/答疑自动切换 JSON Schema 输出；
4. 后续版本路线（PRD 9.3）：多用户/云同步/移动端原生/离线本地模型。
