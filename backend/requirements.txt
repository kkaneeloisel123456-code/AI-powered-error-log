# Recall 后端依赖（FastAPI + SQLAlchemy + Pydantic）
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
sqlalchemy>=2.0.30,<3.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3
alembic>=1.13
httpx>=0.27
python-multipart>=0.0.9
cryptography>=42.0
Pillow>=10.3

# 测试
pytest>=8.2
pytest-asyncio>=0.23

# 可选：本地向量检索（未安装时自动降级为内置关键词检索）
# chromadb>=0.5
